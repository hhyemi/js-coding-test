## 최대공약수와 최소공배수

<hr/>

### :heavy_check_mark: 내가 푼 답안

```javascript
function solution(n, m) {
  function gcd(num1, num2) {
    if (num2 == 0) return num1;
    else return gcd(num2, num1 % num2);
  }

  let result = gcd(n, m);

  return [result, (n * m) / result];
}
```

유클리드 호제법을 이용해서 최대공약수를 구해줬다.  
그리고 **_최소공배수 = 두 자연수의 곱 / 최대공약수_**를 이용해서 최소공배수를 구해줬다.

참조 : [https://myjamong.tistory.com/138](https://myjamong.tistory.com/138)

<br/>
